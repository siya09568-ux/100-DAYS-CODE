#include<stdio.h>
/* Write a program to calculate area of square.*/

int main() {
	
	int side;
	printf("Enter side of square");
	scanf("%d",&side);
	
	printf("area is %d \n", side*side);
	return 0;
}
